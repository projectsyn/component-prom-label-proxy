parameters:
  kapitan:
    compile:
      - input_paths:
          - prom-label-proxy/component/app.jsonnet
        input_type: jsonnet
        output_path: .
      - input_paths:
          - prom-label-proxy/component/main.jsonnet
        input_type: jsonnet
        output_path: prom-label-proxy/
